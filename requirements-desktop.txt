# Desktop UI on top of the core dependencies. Pulls requirements.txt in
# rather than restating requests/aiohttp/apscheduler, which previously had
# to be version-bumped in two files at once.
-r requirements.txt
PySide6>=6.11,<7
